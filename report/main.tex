% The report. This is where a measurement becomes a claim, which is the step at
% which this project's characteristic failure -- a wrong belief -- actually
% happens. Every number that reaches this document has left the record that
% carried its conditions with it. See claims-verified.md, beside this file, for
% the pass that checks it back against source, and read that file's header
% BEFORE writing prose rather than after.
%
% PROSE IS DRAFTED IN MARKDOWN, COMMITTED, AND TYPESET SEPARATELY, WITH BOTH
% COMMITTED SO THE TRANSLATION IS DIFFABLE. Keep draft.md beside this file.
%
% draft.md IS TRACKED. Not a scratch file, not gitignored, not kept on one
% machine -- committed, and committed in the same pull request as the .tex it was
% typeset into. UNTRACKED, THE DIFF HAS NOTHING TO COMPARE AGAINST AND THE
% PRACTICE IS DECORATIVE: the whole value is that two revisions of the prose sit
% in the history side by side, so that the sentence which changed on its way into
% LaTeX is visible as a change rather than as a fact.
%
% draft.md IS PRESENT AND IS THE PROSE OF RECORD. This .tex file is still the
% scaffold: typesetting is step 4 in docs/state.yaml and is GATED ON THE
% CLAIMS-VERIFICATION PASS (od-001) by the stopping rule. Do not typeset ahead of
% the pass -- the whole value of the split is that claims are corrected while the
% prose is still cheap to move.
%
% This is what made claim verification possible in the instance this template
% came from. Two things follow from it, and they are the reason it is a practice
% rather than a preference:
%
%   1. Claims are checked against the record while the prose is still CHEAP TO
%      MOVE. A paragraph that has to be re-typeset to be corrected is a paragraph
%      whose correction gets argued about.
%   2. THE DIFF BETWEEN DRAFT AND TYPESET OUTPUT IS WHERE A MANGLED NUMBER SHOWS.
%      A figure that changes in translation -- a dropped qualifier, a transposed
%      digit, a scope clause that did not survive being turned into a sentence --
%      is invisible in either document alone and obvious between them.
%
% Build:  make -C report        (or: latexmk -pdf main.tex, from this directory)
%
% IT COMPILES AS SHIPPED, deliberately: a scaffold whose build is broken on day
% one teaches you to ignore the build.

\documentclass[11pt]{article}

% Standard margins, stated rather than defaulted, because figures are sized
% against \textwidth. At letterpaper with 1 in margins \textwidth is exactly
% 6.5 in, which is the width report/figures/README.md asks figure scripts to draw
% to, so a figure included at width=\textwidth is reproduced 1:1 and its type is
% not rescaled.
\usepackage[letterpaper,margin=1in]{geometry}

\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage[hidelinks]{hyperref}

% A MONOSPACE PATH CANNOT HYPHENATE, and this document is full of them. The one
% overfull box the first full build produced was exactly that -- \texttt{} around
% report/claims-verified.md, 17.6pt too wide, with no break point TeX would take.
% It reads fine in Markdown, where nothing justifies.
%
% \allowbreak after the slash was tried first and did NOT take -- recorded
% because the obvious fix failing is the useful half. \sloppy, scoped to the one
% block that contains the path, does. It is scoped rather than global: one box in
% twenty-two pages does not justify loosening how the whole document is set. If
% paths multiply, revisit -- seqsplit, or hyphenat with [htt], is the general
% answer, and either would add a package to the CI install list.

% NO BIBLIOGRAPHY, and that is a finding rather than an omission. The draft cites
% no published work -- checked, not assumed: no author name, no "et al.", no
% year-in-parentheses anywhere in report/draft.md. The four repositories in
% Appendix A are named in prose, not cited. Rather than invent entries, biblatex
% is commented out here and refs.bib holds its rule and no entries.
%
% WHAT WOULD GO IN IT, when it is wanted: four @software entries, one per
% repository in Appendix A, each pinned to the SHA now recorded in
% docs/inherited-measurements.yaml -- bio-3d-vision 5f8e39f, math-ai-method
% 81a638e, active-stereo 3f7a263, bioeye e908170. That is the only citation form
% that means anything for a repository, and refs.bib says why.
%
% \usepackage[backend=biber,style=numeric,sorting=none]{biblatex}
% \addbibresource{refs.bib}

\graphicspath{{figures/}}

\title{Personal Notes on a Methodology for Research and Development\\
       in Applied Computational Mathematics}

% AUTHORSHIP IS A CLAIM LIKE ANY OTHER. If an AI collaborator did work that
% appears here, say what it did and where, rather than leaving the reader to
% infer it. The instance this template came from named its collaborator on the
% byline with a footnote pointing at an appendix that described what each surface
% actually did. Adopt that, or decide otherwise deliberately -- but decide.
% Taken from report/draft.md, which names the collaborator on the byline with a
% footnote, and devotes section 8.4 to the recursion rather than disclaiming it.
% That is the deliberate decision this comment block asks for.
\author{
  Luiz Velho \\
  \normalsize IMPA
  \and
  Claude\thanks{These notes were produced in collaboration with Claude ---
    model Opus, high reasoning effort. See \S8.4, which takes the recursion as a
    subject rather than a disclaimer.} \\
  \normalsize Anthropic \\
  \normalsize \textit{collaborator}
}

\date{\today}

\begin{document}
\maketitle

% NO ABSTRACT. The draft has none, and writing one here would be composing prose
% that has never been through a claims pass -- in the passage the template
% identifies as the most dangerous in a report, because it is furthest from the
% record and most rewarded for compression. The status note below is the draft's
% own, typeset from it.
\begin{quote}
\small\sloppy
\textbf{Draft status.} This document was drafted on the conversational surface
and assembled by hand. Its factual claims about the source project have now been
through \textbf{one} claims-verification pass --- the practice \S4.1 describes
and \S7 ships --- covering \S\S1--8.5, resolved against pinned checkouts of the
repositories it makes claims about.

\textbf{42 claims: 22 confirmed, 5 corrected, 8 under-qualified, 6 unsupported,
1 partial} (\texttt{mn-004}). The thirteen corrected and under-qualified claims
have been applied, across twenty passages. Every verdict, its source, and the
sentence it replaced are in \texttt{report/claims-verified.md}.

\textbf{Six claims remain UNSUPPORTED and are unchanged in the prose.} They are
not contradicted by the record; they are unestablishable from it, and four of the
six describe events this method guarantees will leave no artifact --- see
\texttt{gap-004} and \texttt{od-006}. Read \S4.2's rejection count and \S4.5's
cold-session search as recollection, not record.

\S6's \textit{The method destroys its own best evidence} was written after that
pass and its five checkable claims were verified before insertion --- all
confirmed, recorded as A1--A5.

Sections 8.6, 8.7 and 8.8 are the first author's, written in the first person.
They make no checkable factual claims and are out of scope for that pass.
\end{quote}

% GENERATED FROM report/draft.md, one file per numbered section. The draft is the
% prose of record and these are its typeset form; the converter is deterministic
% and the transit was checked word-for-word (11,628 words in, 11,628 out, zero
% differing blocks). Edit the draft, re-run the conversion, and commit both --
% the diff between them is where a mangled figure shows.
\input{sections/01-what-this-is}
\input{sections/02-the-setting}
\input{sections/03-the-project}
\input{sections/04-how-it-goes-wrong}
\input{sections/05-what-it-cost}
\input{sections/06-where-it-failed}
\input{sections/07-the-template}
\input{sections/08-reflection}

\appendix
\input{sections/09-appendix-availability}

\end{document}
